<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet for audio playback and geocoding -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Notification permissions (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Exact alarm for on-time adzan notifications (Android 12+).
         SCHEDULE_EXACT_ALARM requires runtime permission on API 31+;
         USE_EXACT_ALARM is restricted to calendar/clock apps on Play Store. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- Boot completed so notifications survive reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Notification extras -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Battery optimisation exemption (shows system dialog asking for unrestricted bg) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- FOREGROUND_SERVICE permissions kept for awesome_notifications +
         just_audio_background. FOREGROUND_SERVICE_SPECIAL_USE removed
         alongside PrayerForegroundService removal (1.2.1+20). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- Background audio (murottal / adzan playback) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <!-- Google Advertising ID (required for AdMob personalized ads on Android 13+).
         google_mobile_ads ^5.x stopped auto-declaring this permission, so we
         declare it explicitly here. Must match Play Console "Advertising ID"
         declaration (Yes + Advertising/marketing + Personalization). Without
         this, advertising_id is zeroed on Android 13+ and AdMob falls back to
         non-personalized ads with significantly lower eCPM. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:label="Waktu Shalat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- AdMob App ID — Frosthoot Studio production App ID (registered 2026-04-27).
             Test ID was ca-app-pub-3940256099942544~3347511713 (kept for reference).
             REQUIRED by Google Mobile Ads SDK; app crashes on init without it. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4236028330675226~1941898552" />

        <!-- Small home screen widget (2×2) -->
        <receiver
            android:name=".PrayerWidgetSmallProvider"
            android:exported="true"
            android:label="Waktu Shalat (Kecil)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_small_info" />
        </receiver>

        <!-- Medium home screen widget (4×2) -->
        <receiver
            android:name=".PrayerWidgetMediumProvider"
            android:exported="true"
            android:label="Waktu Shalat (Medium)">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_medium_info" />
        </receiver>

        <!-- PrayerForegroundService DECLARATION REMOVED in 1.2.1+20.
             Was causing ForegroundServiceDidNotStartInTime crashes on
             Android 12-14 due to manifest/runtime type mismatch.
             Notifications now rely on awesome_notifications exact alarms +
             WorkManager. Kotlin class file PrayerForegroundService.kt kept
             in tree for git history but is no longer wired up. -->
        <!-- removed: <service android:name=".PrayerForegroundService" .../> -->

        <!-- Reschedule notifications after reboot or app update -->
        <receiver
            android:name="me.carda.awesome_notifications.core.broadcasters.receivers.DartBackgroundService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- Background audio service (murottal / just_audio_background) -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
